{
  "name": "test-app",
  "version": "0.1.0",
  "scripts": {
    "build": "tsc --build",
    "build:watch": "tsc --build --watch",
    "test": "jest",
    "test:watch": "jest --watchAll",
    "start": "node -r dotenv/config dist/index.js",
    "dev": "tsnd -r dotenv/config --transpile-only --respawn ./src/index.ts --rs --deps"
  },
  "dependencies": {
    "axios": "^0.21.0",
    "back-end": "workspace:back-end",
    "common-tags": "^1.8.0",
    "discord.js": "^12.5.1",
    "dotenv": "^8.2.0",
    "front-end": "workspace:front-end",
    "node-emoji": "^1.10.0",
    "shared": "workspace:shared",
    "typeorm": "^0.3.0-rc.21"
  },
  "engines": {
    "node": ">=12.0.0"
  },
  "devDependencies": {
    "@types/common-tags": "^1.8.0",
    "ts-node-dev": "^1.1.1",
    "typescript": "^4.1.3"
  }
}