{
  "name": "@framedjs/shared",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "version": "0.3.0-next.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/some1chan/framed.js.git",
    "directory": "packages/shared"
  },
  "scripts": {
    "build": "tsc --build",
    "build:watch": "tsc --build --watch",
    "test": "jest",
    "test:watch": "jest --watchAll"
  },
  "files": [
    "dist/**/*.js",
    "dist/**/*.d.ts"
  ],
  "dependencies": {
    "require-all": "^3.0.0"
  }
}