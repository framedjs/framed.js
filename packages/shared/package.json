{
  "name": "shared",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "version": "0.5.0",
  "scripts": {
    "build": "tsc --build",
    "build:watch": "tsc --build --watch",
    "start": "pnpm build && node dist/index.js",
    "dev": "tsnd --inspect --transpile-only --project tsconfig.json --respawn -r tsconfig-paths/register ./src/index.ts --rs"
  },
  "dependencies": {
    "colors": "^1.4.0",
    "discord.js": "^12.3.1",
    "winston": "^3.3.3"
  }
}